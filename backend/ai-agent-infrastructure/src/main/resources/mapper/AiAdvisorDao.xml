<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dasi.infrastructure.persistent.dao.IAiAdvisorDao">
    <resultMap id="dataMap" type="com.dasi.infrastructure.persistent.po.AiAdvisor">
        <id column="id" property="id"/>
        <result column="advisor_id" property="advisorId"/>
        <result column="advisor_name" property="advisorName"/>
        <result column="advisor_type" property="advisorType"/>
        <result column="advisor_param" property="advisorParam"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryByAdvisorId" parameterType="java.lang.String" resultMap="dataMap">
        SELECT
            id,
            advisor_id,
            advisor_name,
            advisor_type,
            advisor_param,
            create_time,
            update_time
        FROM ai_advisor
        WHERE advisor_id = #{advisorId}
    </select>

    <select id="page" resultMap="dataMap">
        SELECT id,
               advisor_id,
               advisor_name,
               advisor_type,
               advisor_param,
               create_time,
               update_time
        FROM ai_advisor
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    advisor_id LIKE CONCAT('%', #{keyword}, '%')
                    OR advisor_name LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
        ORDER BY update_time DESC, id DESC
        <if test="offset != null and size != null">
            LIMIT #{offset}, #{size}
        </if>
    </select>

    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM ai_advisor
        <where>
            <if test="keyword != null and keyword != ''">
                AND (
                    advisor_id LIKE CONCAT('%', #{keyword}, '%')
                    OR advisor_name LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
    </select>

    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM ai_advisor
    </select>

    <insert id="insert" parameterType="com.dasi.infrastructure.persistent.po.AiAdvisor">
        INSERT INTO ai_advisor (advisor_id,
                                advisor_name,
                                advisor_type,
                                advisor_param)
        VALUES (#{advisorId},
                #{advisorName},
                #{advisorType},
                #{advisorParam})
    </insert>

    <update id="update" parameterType="com.dasi.infrastructure.persistent.po.AiAdvisor">
        UPDATE ai_advisor
        <set>
            <if test="advisorId != null">advisor_id = #{advisorId},</if>
            <if test="advisorName != null">advisor_name = #{advisorName},</if>
            <if test="advisorType != null">advisor_type = #{advisorType},</if>
            <if test="advisorParam != null">advisor_param = #{advisorParam},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE
        FROM ai_advisor
        WHERE id = #{id}
    </delete>
</mapper>
